# Semantic Similarity

To run your own test, replace path's to files to compare in index.py lines 4 & 5. 

## To Run
Install dependencies:
  `pip install sentence_transformers`

To run: `python3 index.py`